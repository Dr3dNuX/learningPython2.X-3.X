import os
"""while True:
    command = input('>> ')
    if command == 'exit':
        break
    else:
        for line in os.popen(command):
            print(line)
            """
d = os.name

print(d)